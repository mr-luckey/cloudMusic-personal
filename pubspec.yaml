name: blackhole
description: An Open Source Music Player App.

# Prevent accidental publishing to pub.dev.
publish_to: "none"

version: 17.0.0

environment:
  sdk: ">=3.1.0 <4.0.0"
  # flutter: ">=3.3.0"

dependencies:
#  admob_flutter: ^3.0.0
  app_links: ^7.0.0
  cached_network_image: ^3.3.1
  audio_service: ^0.18.19

  
  carousel_slider: ^5.1.2
  connectivity_plus: ^7.0.0
  cupertino_icons: ^1.0.9
  dart_des: ^1.0.2
  device_info_plus: ^12.3.0
  # ffmpeg_kit_flutter_audio: ^6.0.3
#  fading_edge_scrollview: ^3.0.0
  # facebook_audience_network: ^1.0.1
  file_picker: ^10.3.10
  flip_card: ^0.7.0
  flutter:
    sdk: flutter
  flutter_archive: ^6.0.3
  flutter_displaymode: ^0.7.0
  # flutter_downloader: ^1.7.1
  flutter_localizations:
    sdk: flutter
  flutter_lyric: ^2.0.4+6
    # git:
    #   url: https://github.com/Sangwan5688/flutter_lyric
    #   ref: 8cea262
  equatable: ^2.0.7
  flutter_bloc: ^9.1.1
  get: ^4.6.6
  get_it: ^8.3.0
  # google_mobile_ads: ^4.0.0

  google_mobile_ads: ^6.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  # home_widget: ^0.3.0
  html_unescape: ^2.0.0
  just_audio: ^0.10.5
  # Discontinued, Use just_audio_windows instead.
  # For Linux use just_audio_mpv.
  # just_audio_libwinmedia: ^0.0.4 # For windows and linux.
  logging: ^1.3.0
  marquee: ^2.3.0
  material_design_icons_flutter: ^7.0.7296

  native_ads_flutter: ^1.0.7
  on_audio_query: ^2.9.0
  package_info_plus: ^9.0.0
  palette_generator: ^0.3.3+6
  path_provider: ^2.1.5
  permission_handler: ^12.0.0+1
  persistent_bottom_nav_bar: ^6.2.1
    # git:
    #   url: https://github.com/Sangwan5688/PersistentBottomNavBar
    #   ref: ecdadb0
  receive_sharing_intent: ^1.9.0
  share_plus: ^12.0.0
  sizer: ^3.1.3
  sliding_up_panel: ^2.0.0+1
  upgrader: ^12.5.0
  url_launcher: ^6.3.1
  uuid: ^4.5.1
  # workmanager: ^0.5.1
  # Keep 2.x — 3.x breaks current YouTube stream playback path.
  youtube_explode_dart: ^2.5.3
  audiotagger:
    git:
      url: https://github.com/Samurai016/Audiotagger.git
  gif: ^2.3.0
  facebook_audience_network: ^1.0.1
    #  ref: 90f2db5 
    #  url: https://github.com/Samurai016/Audiotagger.git
    #  ref: 90f2db5
dependency_overrides:
  fading_edge_scrollview: ^4.1.1
  persistent_bottom_nav_bar: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.3.0
  msix: ^3.16.8

flutter:
  uses-material-design: true

  # Enable generation of localized Strings from arb files.
  generate: true

  assets:
    # Add assets to the application.
    - assets/

msix_config:
  display_name: CloudSpot
  publisher_display_name: AppWare Technologies
  identity_name: com.appware.cloudSpot
  icons_background_color: transparent
  logo_path: assets/ic_launcher.png
  file_extension: .mp3, .m4a
