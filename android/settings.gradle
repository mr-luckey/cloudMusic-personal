// Settings file migrated to modern Gradle/AGP syntax.

pluginManagement {
    // Load Flutter SDK path from local.properties inside this block (allowed)
    def localProperties = new Properties()
    def localPropertiesFile = new File(rootDir, 'local.properties')
    if (localPropertiesFile.exists()) {
        localPropertiesFile.withReader('UTF-8') { reader ->
            localProperties.load(reader)
        }
    }
    def flutterSdkPath = localProperties.getProperty('flutter.sdk')

    if (flutterSdkPath != null) {
        includeBuild("${flutterSdkPath}/packages/flutter_tools/gradle")
    }
    repositories {
        // Flutter repository for plugin resolution
        def flutterPath = localProperties.getProperty('flutter.sdk')
        if (flutterPath != null) {
            maven { 
                url = uri("${flutterPath}/bin/internal/repo")
            }
        }
        // Flutter remote repository as fallback
        maven {
            url = uri("https://storage.googleapis.com/download.flutter.io")
        }
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    // Required for Flutter Gradle integration
    id "dev.flutter.flutter-plugin-loader" version "1.0.0"

    // Plugin versions centralized here
    id "com.android.application" version "8.6.0" apply false
    id "org.jetbrains.kotlin.android" version "2.1.0" apply false
    id "com.google.gms.google-services" version "4.4.2" apply false
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        // Flutter local repository must be first to resolve io.flutter artifacts
        def localProps = new Properties()
        def lpFile = new File(rootDir, 'local.properties')
        if (lpFile.exists()) {
            lpFile.withReader('UTF-8') { r -> localProps.load(r) }
        }
        def flutterPath = localProps.getProperty('flutter.sdk')
        if (flutterPath != null) {
            maven { 
                url = uri("${flutterPath}/bin/internal/repo")
            }
        }
        // Flutter remote repository as fallback
        maven {
            url = uri("https://storage.googleapis.com/download.flutter.io")
        }
        google()
        mavenCentral()
    }
}

include ':app'
